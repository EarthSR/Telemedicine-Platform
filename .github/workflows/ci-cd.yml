name: Test Runner
on: [workflow_dispatch]

jobs:
  test:
    runs-on: self-hosted
    steps:
      - name: Say hello
        run: echo "Hello from self-hosted runner on $(hostname)"

      - name: Check Docker
        run: |
          docker --version
          docker compose version
